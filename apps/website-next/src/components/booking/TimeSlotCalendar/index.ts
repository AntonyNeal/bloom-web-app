/**
 * TimeSlotCalendar - Weekly appointment booking calendar
 * 
 * Features:
 * - Fetches availability from Halaxy API
 * - Auto-advances to first week with availability
 * - Mobile-first responsive design (tabs on mobile, grid on desktop)
 * - Auto-refreshes every 60 seconds
 */

export { TimeSlotCalendar } from './TimeSlotCalendar';
export type { TimeSlotCalendarProps, TimeSlot, DaySchedule } from './types';
